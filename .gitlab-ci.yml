default:
  image: python:3.13-alpine
  tags:
    - veritas

pages:
  stage: deploy
  script:
  - pip install --upgrade .[docs]
  - cd docsrc
  - make html
  - mv --update _build/html/* public
  artifacts:
    paths:
    - public
  only:
  - pages
