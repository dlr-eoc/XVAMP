# define stages explicitly
stages:
  - deploy

# set pip cache to be outside the Docker volume
variables:
  PIP_CACHE_DIR: "/data-common/tmp/pip-cache/"

# default settings for jobs
default:
  image: condaforge/mambaforge:latest
  tags:
    - mf-sar-dev

# build the documentation
pages:
  stage: deploy
  script:
  - which python; python --version
  - which conda; conda --version
  - conda init
  - source /root/.bashrc
  - conda config --show
  - conda env create --prefix=.condaenv -f environment.yml
  - conda activate .condaenv
  - which python; python --version
  - python -m pip install --upgrade .[docs]
  - cd docsrc
  - make html
  - mv _build/html ../public
  artifacts:
    paths:
    - public
  only:
  - pages
